$(document).ready(function () {
    var CRSL_1 = $(".modular__kitchen-solutions-carousel");
    var CRSL_2 = $(".modular__wardrobe-storage-solutions-carousel");
    CRSL_1.owlCarousel({
      nav: false, // Disable default navigation
      dots: false, // Disable dots
      rtl: true, // Right-to-left direction

    });
    CRSL_2.owlCarousel({
        nav: false, // Disable default navigation
        dots: false, // Disable dots
        rtl: true, // Right-to-left direction
  
      });

  });
  